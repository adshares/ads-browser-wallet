doctype html

html
  head
    meta(charset='UTF-8')
    title ADS Wallet Plugin
    style.
      html, body {
        margin: 0 auto;
        padding: 0;
      }
      body {
        width: 400px;
        max-height: 580px;
      }

  body
    #root
    script(src=env == 'prod' ? '/js/adswallet.bundle.js' : 'http://localhost:3000/js/adswallet.bundle.js')
